//log all even numbers from 1 - 100
//– create a function that returns the square of a number then return all squares from 1 -10

var i = 1;
while (i < 10) {
    console.log(i * 2); // add -1 to get odd
    ++i
}

/*
 * the code can also be written this way
 * var i = 1;
 while (i < 10) {
 if(i % 2 === 0)
    console.log(i);
    ++i
}
 */

function squareNum(input) {
    return input ** a; // return input * input
}

while (i > 11){
    console.log(squareOfNumber(input));
    input++
}

//log “The number is <i>” for numbers 1-25
// –    subtract by 10 from 100 until you reach -50.

var i = 1;
do{
    console.log("The number is " + i); ++i

} while (i < 26);

var i = 100;
do {
    console.log(i - 10);
    i -= 10;
} while (i >= -50);

// the break command will not break out of a function (sans the Switch conditional) if loop is placed in a function.
// remember ->initialize, conditional, and increment to create loops

/**Fizz Buzz: log all numbers from 1 - 100
 –    if divisible by 3 log “Fizz"
 –    if divisible by 5 log “Buzz"
 –    if divisible by 3 and 5 log “FizzBuzz”
 –    else, log the current number
 **/
//var i = 1;
//function isDiv(input){
// return input + 1;
//}
// you can also use conditionals inside of loops to add more inputs
// in this example, you can use if/elseif within the loop
for (var i = 1; i <= 100; ++i) {
    if
        console.log();

}
